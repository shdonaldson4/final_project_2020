---
title: "Final Project Coding"
author: "Sarah Donaldson, Xiaoyu Liu, Vinita Vader"
date: "11/7/2020"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)

library(tidyverse)
library(rio)
library(here)
library(janitor)
library(papaja)
library(tinytex)
library(knitr)
library(psych)
```

## Tidying Data

```{r data loading}
# Step 1: Import
raw_data = import(here("data", "big5_data.csv"), setclass = "tbl_df")
kable(head(raw_data), caption = "Brief look at the dataset" )

# Step2: Use ONLY US cases
tidy_data = raw_data %>% 
  filter(country == "US")

tidy_data
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
